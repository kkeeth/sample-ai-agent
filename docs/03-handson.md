# 事前課題 ── 45 分

> ⚠️ 合宿の前に，各自でやってきてください． 当日にこの時間はありません．
>
> Day 1 の持ち時間は 13:00–17:00 の 240 分だけで，そのうち開発に使えるのは 150 分です．ここを当日にやると，開発時間が 3 分の 2 になります．
>
> 当日 13:15 に 15 分だけ「全員で 1 回動かす」時間を取りますが，そこで拾えるのは環境が壊れている人までです．中身は各自で通してきてください．

| | 時間 | やること |
| --- | --- | --- |
| 1 | 15 分 | ループを読んで理解する（[これだけ](00-tldr.md)） |
| 2 | 10 分 | 自分の手元で 1 回動かす |
| 3 | 20 分 | ツールを 1 個足して，呼ばれないのを見て，直す |

2 まで（`bun run check` が通る）は必須です． 3 まで終わっていると，当日そのまま開発に入れます．

> 詰まったら当日まで放置せず，運営の Slack チャンネルに書いてください． 当日の朝に持ち込まれると，そのチームの開発時間が削れます．

---

## 1. ループを読む ── 15 分

[これだけ](00-tldr.md) を読んでから，`src/agent.ts` を開いて 30 行のループを上から順に目で追ってください．押さえるのは 4 つだけです．

1. ループが 1 周するたびに，これまでの全部をもう一度送っている
2. モデルは「このツールを呼べ」と言うだけで，呼ぶのはこちらのコード
3. だから承認ゲートもログもコスト計測も，全部この 1 箇所に入る
4. 参加者が書くのは `src/tools.ts` の 3 つだけ（`SYSTEM_PROMPT` / `tools` / `runTool`）

> ここが腑に落ちなくても構いません． 3 で手を動かすと分かります．先に進んでください．

---

## 2. 動かす ── 10 分

ここが通らないうちは 3 に進まないでください． 通らない場合は運営に連絡してください．

```bash
bun install
cp env.example .env        # 運営から配られたキーを貼る
bun run check              # 疎通確認
```

`bun run check` が通ったら，エージェントを 1 回動かします．

```bash
bun run agent "今日は何日?" -v
```

`-v` は，モデルが何を考えて次のツールを選んだかを表示するオプションです．

出力のここを見てください．

```
  [1周目] in 1,234 / out 89 / 累計 $0.0028      ← 1周ごとのトークンとコスト
  [考え中] 現在時刻が必要なのでツールを使う      ← -v で出る
  → get_current_time({})                        ← モデルが「呼べ」と言い、こちらが実行した
  [2周目] in 1,410 / out 45 / 累計 $0.0041      ← 入力が増えている＝全履歴を再送している
```

2 周目の入力トークンが 1 周目より増えているのが見えますか． これが「毎回まるごと送り直している」ということです．

> 詰まったときは，ほぼ全部この 3 つのどれかです．
>
> | 症状 | 原因 |
> | --- | --- |
> | `✗ 認証情報が見つかりません` | `.env` を作っていない／キーを貼っていない |
> | `400 credit balance` | 残高かワークスペースの spend limit．運営側の問題 |
> | ネットワークに届かない | 会場の回線・プロキシ．前日に実機で確認しておくこと |

---

## 3. ツールを 1 個足す ── 20 分

合宿の当日，やり続けるのはこの作業です．先に 1 回やっておいてください．

### 3-1. わざと雑な説明で足す（10 分）

`src/tools.ts` を開いて，2 箇所に足します．

① 名簿（`tools` 配列）に追加

```ts
  {
    name: "days_until",
    description: "日数を計算する",          // ← わざと雑にしてあります
    input_schema: {
      type: "object",
      properties: {
        date: { type: "string" },
      },
      required: ["date"],
    },
  },
```

② 中身（`dispatch` の `switch`）に追加

```ts
    case "days_until": {
      const target = new Date(args.date ?? "");
      if (Number.isNaN(target.getTime())) {
        return `日付を解釈できませんでした: ${args.date}`;
      }
      const days = Math.ceil((target.getTime() - Date.now()) / 86_400_000);
      return `${args.date} まで あと ${days} 日`;
    }
```

動かします．

```bash
bun run agent "今年の大晦日まであと何日?" -v
```

何が起きたかをメモしてください． チームによって違うものが見えるはずです．

- そもそも `days_until` を呼んだか，呼ばなかったか
- `get_current_time` を先に呼んで今日の日付を確かめたか，確かめずに推測したか
- `date` に何を渡してきたか（`2026-12-31` か，`今年の大晦日` か，`2026年12月31日` か）
- エラーが返ったあと，自力で直したか，同じことを繰り返したか

> 運営向け — ここはうまくいかないのが正解です．「動かない」と手が挙がったら成功なので，そう伝えてください．
> 実行するたび結果が変わります．それ自体が「モデルがその場で順番を決めている」ということです．

### 3-2. 説明を直す（10 分）

`description` を書き直します．「何をするか」だけでなく「いつ使うか」「何を渡すか」まで書きます．

```ts
  {
    name: "days_until",
    description:
      "指定した日付まで、今日からあと何日あるかを返す。" +
      "「あと何日」「締め切りまで」のように、未来の日付までの日数を聞かれたときに使うこと。" +
      "今日が何日か分からないときは、先に get_current_time で確認すること。",
    input_schema: {
      type: "object",
      properties: {
        date: {
          type: "string",
          description: "基準となる日付。YYYY-MM-DD 形式。例: 2026-12-31",
        },
      },
      required: ["date"],
    },
  },
```

変えたのは説明文だけです．実装は 1 行も触っていません． もう一度動かします．

```bash
bun run agent "今年の大晦日まであと何日?" -v
```

`get_current_time` → `days_until` の順に呼ぶようになったはずです．

これが今日いちばん大事な一点です．

> モデルは，あなたが書いた説明文しか読んでいません．実装は見えていません．
> ツールを呼んでくれないときは，まず説明文を疑ってください．

数字で見たい人は，変える前と後でこれを走らせると，平均ツール呼び出し回数が比べられます．

```bash
bun run measure "今年の大晦日まであと何日?" 5 src/cli.ts
```

---

## ここまで来たら，よーいドン

あとはこの繰り返しです．

1. どんなツールが要るか決める（3〜5 個に絞る）
2. `tools` に名簿を書く
3. `runTool` に中身を書く ← ただの関数．ここが作業量の大半
4. 動かして，説明文を直す

書き方に詰まったら，動く作例が 3 つあります．丸ごとコピーして，そこから壊すのが最短です．

```bash
cp examples/doc-agent/tools.ts   src/tools.ts   # ローカルのファイルを読む
cp examples/http-agent/tools.ts  src/tools.ts   # HTTP を叩く
cp examples/state-agent/tools.ts src/tools.ts   # 状態を書き換える
```

どれがどんなときに向くかは [examples/README.md](../examples/README.md) にあります．

---

## 手が止まったときの行き先

| 知りたいこと | どこ |
| --- | --- |
| 要点をもう一度 | [これだけ](00-tldr.md) |
| そもそもエージェントとは | [電話越しの現場監督](01-agent-primer.md) |
| ループを自分で書いてみたい | [作る順番](04-reference.md) の Step 0〜2 |
| 書き込み系のツールを足したい | [作る順番](04-reference.md) の Step 7〜8 |
| エラーの意味が分からない | [作る順番](04-reference.md) の「詰まったときの切り分け」 |
| 使い方・ファイル構成 | [README](../README.md) |

---

*関連： [これだけ](00-tldr.md) ／ [キックオフ台本](02-kickoff-script.md)*
